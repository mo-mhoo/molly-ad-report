# 2026-06-20 Session Log

## 完成的事

### 預算排程功能（app.py）
- **合併查看排程 → 新增排程 tab**：從 3 tabs 變 2 tabs，表格內直接顯示今日排程狀態
- **新欄位**：今日ROAS、7天ROAS（date_preset 修正為 `last_7d`，非 `last_7_days`）、今日排程
- **排序**：🟢有花費 → 🟢無花費 → ⏸，同層依今日ROAS desc
- **換 AgGrid**：支援 Shift+click 範圍選取、表頭 checkbox 全選、快速按鈕（全選/取消/選有花費）
- **budget_value 公式確認**：`budget_value = int(pct)` 直接等於增加百分比（300 = +300%）。舊公式 `100+pct` 導致多加 100%，已修正
- **今日排程顯示修正**：`bv-100` → `bv` 直接顯示（舊邏輯基於錯誤的公式假設）
- **建立排程後即時更新**今日排程欄，不需重新載入

## 未完成 / 待確認
- RT｜BAU 有兩筆重疊排程（+25% 和 +300%），需手動從刪除排程 tab 清掉 +25% 那筆

## 踩坑紀錄
- Meta `date_preset` 值是 `last_7d` 不是 `last_7_days`
- Meta MULTIPLIER：budget_value=300 = 增加300%（花費到 4x），之前誤解為 budget_value=100 不變、120 為+20%
- Streamlit data_editor 底層是 canvas，JS 無法注入 checkbox 事件，須改用 AgGrid
- AgGrid checkbox 需在目標欄明確設 `checkboxSelection=True`，`use_checkbox=True` 單獨設定不夠
- `grid_resp.get("selected_rows")` 可能回傳 None，需用 `or []` 防守
