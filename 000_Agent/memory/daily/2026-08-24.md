# 2026-08-24 Session Log

## 今天做了什麼

- **確認毛孩時代「19.全貓飼料-Blog_全貓飼料系列_ATL」加日預算 +3000 已生效**（完成）：Molly 想加預算但一開始沒說清楚是 Meta 還是 Google Ads、也沒說完整活動名稱（只說「ATL 全貓飼料」）。查 Drive 上的「Google Ads修改紀錄_毛孩時代」表和本機 repo 都找不到寫入能力，後來用 change_event 查到她本人已在 8/21 08:14 手動把日預算從 7000 改成 10000，跟現在帳戶即時數字吻合，直接幫她確認完成，不用再猜。
- **建立 Google Ads 日預算調整工具**（完成）：新增 [google_ads_budget_updater.py](google_ads_budget_updater.py)，可查詢/調整活動日預算，不用每次進後台手動改。Molly 明確表示不要用 Adspirer 連接器，所以改成直接寫 Google Ads API mutate 程式碼。
- **釐清毛孩時代 Google Ads 帳號結構**（完成）：MCC「騰勢(股)」`4195240594` 底下有主帳號、蝦皮商城、PfM_45057582、部落格子帳號共 4 個毛孩時代相關帳號，御熹堂是完全不同帳號，不要混用。

## 做法筆記

- 本機沒有 Google Ads API 憑證（只存在 Streamlit Cloud Secrets），也沒有 mutate 程式碼，所以一開始老實跟 Molly 說「這次做不到、只能手動」，而不是硬做或裝懂；Molly 後來主動要求建立寫入能力，才動手寫。
- 憑證放本機 `.env`（已加進 `.gitignore`），用獨立 `.venv`（系統 Python 3.9 太舊）安裝 `google-ads` + `python-dotenv` 執行，不動用 Streamlit Cloud 環境。
- 活動名稱關鍵字模糊（用戶說「ATL 全貓飼料」，帳戶裡實際全名是「19.全貓飼料-Blog_全貓飼料系列_ATL」）+ 帳號結構有多個子帳號時，先用 `--login-customer-id`（MCC）+ 逗號分隔多個 `--customer-id` 做查詢模式掃描，避免像之前礦砂6限搶活動漏抓複製版廣告那樣改錯/改漏（見 [[排程建立前需掃全帳號找複製版]]）。
- Google Ads API 的 `change_event` resource 可以查最近異動紀錄（操作人、修改前後數值），比一直問 Molly「你改成多少」更可靠、更快；但要注意 `change_event.resource_name` 是變更事件本身的 ID 不是被改資源的 resource_name，要看 `old_resource`/`new_resource` 底下對應欄位才是實際數值。
- Drive 的 `read_file_content` 對多分頁大試算表只會回傳部分索引分頁內容，抓不到目標分頁（如「19.全貓飼料」）的實際數字；也沒有寫入 Sheet 內容的工具（`update_file` 只能改標題/位置），所以記錄異動這件事最後還是要 Molly 自己貼進 Google Ads修改紀錄表。

## 未完成 / 待辦

- Molly 需要自己把這次「7000→10000」的異動貼進「Google Ads修改紀錄_毛孩時代」表的「19.全貓飼料」分頁（我沒有寫入權限）。
- 之後如果常改預算，可以考慮幫 `google_ads_budget_updater.py` 加一個自動寫入修改紀錄表的功能——但要先有 Sheet 寫入權限的工具才行，目前沒有。
