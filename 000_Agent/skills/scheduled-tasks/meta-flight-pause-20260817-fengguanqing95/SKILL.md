---
name: meta-flight-pause-20260817-fengguanqing95
description: 走期到期自動暫停：封館慶95折活動廣告（毛孩時代_官網），8/17 00:00 執行
---

你要幫 Molly 把兩支已經到走期的 Meta 廣告改成 PAUSED。這是「活動素材走期到期自動暫停」的一次性排程任務，不需要跟 Molly 對話，直接執行並回報結果。

背景：這兩支廣告的名稱都是 `[~8/16]20260814_活動_影片_全館活動_封館慶95折最高折500_all`，走期是 2026/08/14 起、2026/08/16 到期（含 8/16 全天），所以到 2026/08/17 00:00 就該暫停。

帳號：毛孩時代_官網（account_id 1318362572209550）

要暫停的廣告 ID：
- 120250147089150026（在 adset「舊客｜購買/名單排除PUR(30)」）
- 120250147090520026（在 adset「粉專/IG互動」）

執行步驟：
1. 用 Bash 工具，cd 到 `C:\AI小摸`，用 Python（`requests` + `python-dotenv`，`.env` 裡的 `META_ACCESS_TOKEN`）呼叫 Meta Graph API v21.0：對上面兩個廣告 ID 各發一次 `POST https://graph.facebook.com/v21.0/{ad_id}`，帶 `access_token` 和 `status=PAUSED`。
2. 呼叫前先用 GET 查一次這兩支廣告目前的 `effective_status`——如果已經是 PAUSED（可能 Molly 自己先手動關了），就跳過那支，不要重複呼叫，並在回報裡註明「已是 PAUSED，跳過」。
3. 兩支都處理完後，用一段簡短的文字訊息回報結果：哪支成功暫停、哪支本來就已暫停、有沒有失敗（附錯誤訊息）。

注意：這是寫入類操作，但暫停（PAUSED）方向是安全的降級動作（不會增加花費），且 Molly 已經在建立這兩支廣告時明確要求走期到期要自動暫停，不需要再另外跟她確認才執行。