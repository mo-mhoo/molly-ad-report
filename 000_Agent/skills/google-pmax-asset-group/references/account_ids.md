# 已知帳號 ID

MCC（代理商中心）帳戶 `4195240594`（騰勢(股)）底下掛所有 TSA 集團品牌的 Google Ads 帳號，
這也是 `.env` 裡 `GOOGLE_ADS_LOGIN_CUSTOMER_ID` 的預設值。

| 品牌 | customer_id | 備註 |
|---|---|---|
| 御熹堂 | `8669832537` | 官網主帳號 |
| 毛孩時代 | `1014276621` | 官網主帳號 |
| 毛孩時代（部落格子帳號） | `5079367835` | `PfM_45057582_毛孩時代`，後台顯示 ID 為 507-936-7835；官網花費要合併這兩個帳號才完整，見 [[project_google_ads_integration]]。底下有蝦皮相關 PMax 活動（例：`BTL｜2501_蝦皮pmax_all #2`）。 |

其他品牌（大牧本草、PowerHero、Tryme 等）目前沒有確認過的 customer_id，
**遇到時直接問 Molly 要哪個帳號，不要用名稱猜**——這個帳號體系裡光御熹堂／毛孩時代
就有好幾個容易搞混的相似帳號名稱（例如「毛孩時代-蝦皮商城」是閒置帳號，真正在投放的
是另一個叫 `PfM_45057582_毛孩時代` 的帳號），猜錯的代價是查到錯誤數據或寫入錯帳號。

**注意（2026-08-18 校正）：`PfM_45057582_毛孩時代` 先前記錯成 `1026904578`，
經後台截圖核對，正確 customer_id 是 `5079367835`（後台顯示 507-936-7835）。
`1026904578` 這個 ID 底下完全沒有 PMax 活動，只有 Search/Display，容易誤判成
「這帳號沒有蝦皮活動」，遇到類似情況務必以後台截圖或 UI 顯示的 ID 為準重新核對，
不要只信舊筆記。

背景細節見 memory [[project_google_ads_integration]]。
