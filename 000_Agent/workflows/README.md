# workflows/ — 你每天主動喊的固定儀式

這個資料夾放「你手動打一次、AI 就跑一整套流程」的多步驟工作流，例如 `/morning`、`/journal`、`/weekly-report`。

## 跟 skills/ 差在哪？

- **skills** 是「方法論 + SOP」，會被其他任務引用（例如寫作技巧、廣告週報格式）
- **workflows** 是「每天的固定儀式」，會**串接多個 skill** 一次跑完

例如：
> `/weekly-report` → 整理廣告數據 → 寫 PDCA 分析 → 輸出週報草稿

> 詳細機制見迷你課 2-4。
